import { FormatResponsePipe } from './format-response.pipe';

describe('FormatResponsePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatResponsePipe();
    expect(pipe).toBeTruthy();
  });
});
